{
  "name": "pomodone",
  "version": "2.0.0",
  "private": true,
  "description": "Personal productivity PWA — tasks, Pomodoro sessions, goals and analytics.",
  "scripts": {
    "test": "node tests/run.js",
    "test:verbose": "node tests/run.js --verbose"
  },
  "devDependencies": {
    "css-tree": "^3.2.1",
    "jsdom": "^29.1.1"
  }
}
